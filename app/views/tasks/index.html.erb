<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task) %></li>
  <%# task_path(task) pass the id of task to the :id in the url %>
  <% end %>
</ul>
<hr>
<%= link_to "+ New task", new_path %>
