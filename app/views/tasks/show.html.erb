<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p>This task is <%= @task.completed ? 'completed' : 'not completed yet'%></p> <%= link_to "Edit it", edit_path(@task) %>
<%= link_to "Delete it", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}  %>
<%= link_to "Go back", tasks_path %>
